import 'package:flutter/material.dart';

class AppColors {
  // Cores principais - mais suaves e modernas
  static const Color primary = Color(0xFF5E5CE6); // Roxo suave (moderno, profissional)
  static const Color secondary = Color(0xFF00C2A0); // Verde-água (toque de cor)
  static const Color accent = Color(0xFFFF6B6B); // Coral (para alertas/destaques)
  
  // Neutros - base para o minimalismo
  static const Color background = Color(0xFFFFFFFF); // Fundo branco puro
  static const Color surface = Color(0xFFF8F9FA); // Superfície levemente cinza
  static const Color divider = Color(0xFFE9ECEF); // Cinza suave para linhas
  
  // Neutros Dark
  static const Color backgroundDark = Color(0xFF121212);
  static const Color surfaceDark = Color(0xFF1E1E1E);
  static const Color dividerDark = Color(0xFF2C2C2E);

  // Textos - escala de cinza
  static const Color textPrimary = Color(0xFF212529); // Quase preto
  static const Color textSecondary = Color(0xFF6C757D); // Cinza médio
  static const Color textHint = Color(0xFFADB5BD); // Cinza claro
  static const Color textDisabled = Color(0xFFDEE2E6); // Cinza muito claro
  
  // Status
  static const Color success = Color(0xFF40C057); // Verde
  static const Color warning = Color(0xFFFD7E14); // Laranja
  static const Color error = Color(0xFFFA5252); // Vermelho
  static const Color info = Color(0xFF339AF0); // Azul
  
  // Ícones
  static const Color iconPrimary = Color(0xFF5E5CE6); // Roxo (mesmo da primary)
  static const Color iconSecondary = Color(0xFF6C757D); // Cinza
  
  // Badges e tags
  static const Color badgeAdmin = Color(0xFF5E5CE6); // Roxo
  static const Color badgeComercial = Color(0xFF00C2A0); // Verde-água
  static const Color badgeSuporte = Color(0xFFFD7E14); // Laranja
}
